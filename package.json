{
  "name": "@rogerchappel/autochangelog",
  "version": "0.1.0",
  "description": "Local-first TypeScript CLI that generates changelogs from conventional commits.",
  "type": "module",
  "bin": {
    "autochangelog": "./dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "audit": "npm audit --audit-level=low",
    "smoke": "npm run build && node dist/cli.js --help",
    "package:smoke": "node scripts/package-smoke.mjs",
    "package:identity": "node scripts/check-package-identity.mjs",
    "release:check": "npm run audit && npm run package:identity && npm run check && npm test && npm run build && npm run smoke && npm run package:smoke",
    "lint": "npm run check"
  },
  "keywords": [
    "changelog",
    "conventional-commits",
    "release-notes",
    "semver",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/autochangelog.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/autochangelog/issues"
  },
  "homepage": "https://github.com/rogerchappel/autochangelog#readme",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
